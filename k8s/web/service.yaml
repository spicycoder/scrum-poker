apiVersion: v1
kind: Service
metadata:
  name: web-service
  namespace: scrum-poker
  labels:
    app: scrum-poker
    component: web
spec:
  type: ClusterIP
  selector:
    app: scrum-poker
    component: web
  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000
