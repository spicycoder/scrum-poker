name: Mutation Testing

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

jobs:
  stryker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Install Aspire workload
        run: dotnet workload install aspire

      - name: Install Stryker
        run: dotnet tool install dotnet-stryker --tool-path .tools

      - name: Restore
        run: dotnet restore ./ScrumPoker.slnx

      - name: Build
        run: dotnet build --no-restore -c Release ./ScrumPoker.slnx

      - name: Run Stryker
        run: .tools/dotnet-stryker --solution ScrumPoker.slnx --output stryker-output

      - name: Push mutation report to dashboard
        env:
          GH_TOKEN: ${{ secrets.GH_PAGES_TOKEN }}
        run: |
          gh repo clone spicycoder/dashboard /tmp/dashboard
          cd /tmp/dashboard
          git checkout gh-pages
          rm -rf scrum-poker/stryker
          cp -r "$GITHUB_WORKSPACE/stryker-output" scrum-poker/stryker
          git add .
          git -c user.name="CI" -c user.email="ci@spicycoder.dev" \
            commit -m "Update scrum-poker stryker report [skip ci]" --allow-empty
          git remote set-url origin https://spicycoder:${GH_TOKEN}@github.com/spicycoder/dashboard.git
          git push
